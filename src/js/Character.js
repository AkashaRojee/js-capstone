export default class Character {
  constructor(id, name, description, image, url) {
    this.id = id;
    this.name = name;
    this.description = description;
    this.image = image;
    this.url = url;
  }
}